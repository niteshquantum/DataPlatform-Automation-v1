;============================================================
; SQL Server 2022 Configuration Template
; Generated by Data Platform Automation Framework
;============================================================

[OPTIONS]

;------------------------------------------------------------
; Setup Action
;------------------------------------------------------------

ACTION="Install"
ENU="True"

;------------------------------------------------------------
; User Interface
;------------------------------------------------------------

QUIET="True"
QUIETSIMPLE="False"

;------------------------------------------------------------
; Updates
;------------------------------------------------------------

UpdateEnabled="{{UPDATE_ENABLED}}"
USEMICROSOFTUPDATE="False"

;------------------------------------------------------------
; License
;------------------------------------------------------------

IACCEPTSQLSERVERLICENSETERMS="{{ACCEPT_EULA}}"

;------------------------------------------------------------
; Features
;------------------------------------------------------------

FEATURES="{{FEATURES}}"

;------------------------------------------------------------
; Instance
;------------------------------------------------------------

INSTANCENAME="{{INSTANCE_NAME}}"
INSTANCEID="{{INSTANCE_NAME}}"

; Keep all instance and user database files inside the project-managed
; deployment.  These values are resolved from mssql.conf by the generator.
INSTANCEDIR="{{INSTALL_DIR}}"
SQLUSERDBDIR="{{DATA_DIR}}"
SQLUSERDBLOGDIR="{{DATA_DIR}}"
SQLTEMPDBDIR="{{DATA_DIR}}"
SQLBACKUPDIR="{{DATA_DIR}}\backup"

;------------------------------------------------------------
; Security
;------------------------------------------------------------

SECURITYMODE="{{SECURITY_MODE}}"
SAPWD="{{SA_PASSWORD}}"
SQLSYSADMINACCOUNTS="{{SYSADMIN_ACCOUNTS}}"

;------------------------------------------------------------
; SQL Server Service
;------------------------------------------------------------


SQLSVCSTARTUPTYPE="{{SQL_SERVICE_STARTUP}}"

;------------------------------------------------------------
; SQL Server Agent
;------------------------------------------------------------


AGTSVCSTARTUPTYPE="{{AGENT_SERVICE_STARTUP}}"

;------------------------------------------------------------
; Network Configuration
;------------------------------------------------------------

TCPENABLED="{{TCP_ENABLED}}"
NPENABLED="{{NP_ENABLED}}"

;------------------------------------------------------------
; Browser Service
;------------------------------------------------------------

BROWSERSVCSTARTUPTYPE="{{BROWSER_SERVICE_STARTUP}}"

;------------------------------------------------------------
; Full Text Launcher
;------------------------------------------------------------


;------------------------------------------------------------
; FileStream
;------------------------------------------------------------

FILESTREAMLEVEL="0"

;------------------------------------------------------------
; Collation
;------------------------------------------------------------

SQLCOLLATION="SQL_Latin1_General_CP1_CI_AS"
