SERVICE_MODEL=process
PROCESS_NAME=mongod
PROCESS_EXECUTABLE=databases/mongodb/server/bin/mongod

TERRAFORM_DIRECTORY=terraform/mongodb
TERRAFORM_RESOURCES=install_mongodb_linux,start_mongodb_linux

CLEANUP_OWNERSHIP=platform

INSTALLATION_DIR=databases/mongodb
RUNTIME_DATA_DIR=databases/mongodb/data
RUNTIME_TEMP_DIR=outputs/temp/mongodb

LIQUIBASE_ENABLED=false
LIQUIBASE_DIR=
LIQUIBASE_CHANGELOG=

ARTIFACT_OWNERSHIP=cleanup
ARCHIVE_DIR=archive/mongodb
FAILED_DIR=failed/mongodb
INCOMING_DIR=incoming/mongodb
HISTORY_FILE=metadata/mongodb/data_load_history.jsonl

CLEANUP_DATA_ENABLED_UBUNTU=false
RESET_TERRAFORM_ONLY=false
XML_CLEANUP_ENABLED=false
